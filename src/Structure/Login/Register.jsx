import React from 'react'
import { MaterialCommunityIcons } from '@expo/vector-icons'
import { View, Text, StyleSheet, TouchableOpacity,Image, handlePress, TextInput} from 'react-native'
import { View, Text, StyleSheet,Image, handlePress, TextInput , TouchableOpacity} from 'react-native'

import { auth } from './Data/firebase-config';
import { getAuth, createUserWithEmailAndPassword} from 'firebase/auth';

const Register = () => {
    return (
        <View>
            <Text>Wazaa2</Text>
        </View>
    )
}

export default Register